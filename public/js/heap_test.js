// Generated by CoffeeScript 1.10.0
(function() {
  var assert, h, heap, i, j, x;

  assert = require('assert');

  heap = require('./heap.coffee');

  h = new heap.BinaryHeap();

  assert.equal(void 0, h.pop());

  for (x = i = 50; i >= 1; x = --i) {
    h.push(x);
  }

  for (x = j = 1; j <= 50; x = ++j) {
    assert.equal(h.pop(), x);
  }

  assert.equal(void 0, h.pop());

  console.log('all tests ok');

}).call(this);
